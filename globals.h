#pragma once
#include <vector>
#include <raylib.h>

extern const int screenWidth;
extern const int screenHeight;
extern int numBalls;
extern const int minRadius;
extern const int maxRadius;
extern const int minSpeed;
extern const int maxSpeed;
extern const std::vector<Color> myColors;
extern const int minLife;
extern const int maxLife;
